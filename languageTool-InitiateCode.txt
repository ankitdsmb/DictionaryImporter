// In your existing text processing code:
var cleanedText = await GrammarAwareTextCleaner.CleanWithGrammarAsync(
    rawDefinition, 
    applyAutoCorrection: true,
    languageCode: "en-US"
);

// Or for manual grammar checking:
var grammarCorrector = serviceProvider.GetRequiredService<IGrammarCorrector>();
var suggestions = await grammarCorrector.SuggestImprovementsAsync(text);